# MonteCarlo: From classic to quantum
MonteCarlo: From classic to quantum 系列讲义从经典自旋系统的蒙特卡洛算法出发，逐步进入impurity system QMC的算法与用例中，并与dmft等多体计算方法融合、采取复杂动力系统的方法对算法本身的演化进行探讨。

## 说明和原则：
1. 整个系列项目除编写计划外，全部使用英文。
2. 包含tex-beamer为基础的silde部分，refers/ 为保存参考文献和用例的文件夹（应保证引用正确，web-mcp或取得结果应下载后引用到对应位置，如果没有权限则首先arxiv寻找代替，仍然没有则暂停由我手动下载），generatePic.py用于生成需要的示意图供tex引用，流程图应当选取mermaid库，后续还有以cpp为基础的实例代码存在examples/文件夹
3. 项目会逐步推进完成，每次推进只检查并完成当前计划中有详细说明的部分
4. python使用venv虚拟环境，名字为mc_learn（ykxu/uenv/mc_learn），python版本为3.12；cpp使用spack进行包管理（因为主要是科学计算用例，未来有优化空间）
5. generatePic.py将图片保存至pics/$name.png
6. beamer格式应统一，采取令人舒适的学术专用配色
7. 学术严谨原则：出现的所有内容都应当有可供参考查找到文献，可以有数学描述的应当写出数学描述。下载的参考文献以\[序列号\]标题.pdf的形式储存在refers中。如果我的表述有错误，则更正并保留原表述在当前计划文档中，并在编写进度部分进行标记。十分支持网络搜索提高内容准确性和丰富度，但一定要提供具体文献来源，且下载到reders。mcp工具使用minimax。
8. 每个讲义内容排版：1.引子，给出一个模型或应用问题引入主题；2.原理推导；3.算法实例；4.参考文献；5.启发与讨论；。讲义中的序号并不代表页码，只是不同环节。
9. 讲义清晰易读+详细推导原则：主要页的文字不要太多，以流程图、简要公式为主，详细文字、公式推导可以放在该页后面，这样演讲者使用时可以自由选择是否详解。
10. 每个讲义的`0.`部分用于生成或下载所需要的实例图与参考文献
11. 小步迭代原则：为避免上下文幻觉问题，完成一个完整的小部分（一次tex编译成功/一个示例代码运行成功/示例图片输出成功，文字无重叠、超出block/slide，图片显示与预期符合，使用minimax mcp图片理解）就可以停止并记录（记录新增和下次从哪里开始），再等待手动检查、更改 + /clear + /init 后继续进行。小部分仍然过大可以拆分（如算法对比部分有3个算法，可以依次进行上述迭代过程，甚至每个代码输出也可以拆分成更小的可复用的部分依次进行，确保每次编译的时候不超出上下文上限）每次clear+init后都应当先询问上次迭代是否有遗漏问题，以及保持连贯性避免重复赘余。
12. 每个讲义计划中`（）`用于指导操作顺序等，与直接的内容区分开。
13. 用例代码原则：1.注释极度清晰友好，便与讨论；2.考虑抽象接口与扩展性，第一部分的用例在后续仍然可以复用并讨论新的方法（以第一个讲义的wolff为例，第一部分的物理量为平均spin-S，但留好抽象层接口，等到第二部分可以方便地增加cluster overlap作为“可观测量”）；3.做好数据I/O，实例结果应当容易地被generatePic
14. **教学原则**
    - 每次修改bug或错误后都应当说明bug或错误的原因，以及改正的具体位置，让我学会各种工具的使用。小问题的归因于方法也应当记录。
    - 代码部分使用的语法、算法、包管理语法用法都应当储存在techTutoril.md中，claude无需读取，给我自己学习用的。
15. **代码先于slide原则**
    - 每个算法的代码实现必须在编写对应slide之前完成
    - 原因：代码跑通后才会有数值结果图，slide中的实例图应来自实际运行而非手绘示意图
    - 例外：算法流程示意图（如TikZ绘制的cluster生长过程）可与代码并行
16. **spack环境规范**
    - 使用`spack install cppcheck`用于静态分析
    - 使用`spack install googlebenchmark`用于性能对比（Part 2需要）
    - 环境配置命令记录在`examples/README.md`中
17. **QMC架构兼容原则**
    - Part 1的`MCSampler<ConfigType>`模板设计必须支持未来扩展为：
      - 虚时切片序列（DQMC）
      - 变分波函数空间（VMC）
      - 行列式组态空间（FCIQMC）
    - 可通过`ConfigType`特化实现，无需重写核心采样循环
18. **代码规范原则**
    18.1. **命名规范**
        - 类名：PascalCase（如 `IsingModel`, `SwendsenWangSweep`）
        - 函数名：camelCase（如 `computeEnergy()`, `measureMagnetization()`）
        - 变量名：snake_case（如 `lattice_size`, `acceptance_ratio`）
        - 成员变量：后缀下划线（如 `spins_`, `rng_`, `J_`）
        - 常量/枚举：全大写蛇形（如 `MAX_SWEEPS`, `BOND_ACTIVE`）
        - 模板参数：T大写或描述性PascalCase（如 `typename ConfigType`, `typename RNG`）

    18.2. **注释规范**
        - 每个类头部：简要描述 + 参考文献 + 复杂度注解
        ```cpp
        /**
         * @brief Swendsen-Wang cluster update for 2D Ising model
         * @ref  Swendsen & Wang, Phys. Rev. Lett. 58, 86 (1987)
         * @complexity O(N α(N)) per sweep, where α(N) is inverse Ackermann
         */
        class SwendsenWangIsing : public IsingModel { ... };
        ```
        - 每个public函数：功能、参数、返回值、可能的异常
        - 复杂算法内部：关键步骤注释，标注公式编号（对应slide）
        - 禁止：无注释的magic number、无说明的hard-coded参数

    18.3. **接口与抽象**
        - 面向接口编程：算法与模型解耦
        ```cpp
        template<typename ModelType, typename UpdateRule>
        class MCSimulation {
            ModelType model;
            UpdateRule updater;
        public:
            void run(int sweeps);
        };
        ```
        - 依赖注入：随机数生成器、观测器作为可替换组件
        - 禁止：具体算法硬编码在模型类内部

    18.4. **错误处理与调试**
        - 使用断言检查前置条件（`assert(L > 0)`）
        - 关键数值结果输出时附带参数哈希，便于追踪复现
        - 支持详细日志级别（`-v`, `-vv`, `-vvv`）
        - 浮点数比较：使用容差（`std::abs(a-b) < 1e-9`），禁止直接`==`

    18.5. **性能考量**
        - 热循环中避免动态内存分配（预分配buffer）
        - 随机数生成器使用`std::mt19937_64`，局部缓存而非全局调用
        - 关键路径考虑SIMD友好布局（如AoS → SoA转换）
        - Union-Find实现路径压缩 + 按秩合并
        - 允许编译期多态（CRTP模式）替代虚函数开销
        ```cpp
        // 编译期多态示例 — 零开销抽象
        template<typename Derived>
        class MCSamplerCRTP {
        public:
            void sweep() { static_cast<Derived*>(this)->sweep_impl(); }
        };
        ```

    18.6. **可复现性**
        - 随机种子可设置（默认`std::random_device`）
        - 所有运行时参数记录到输出文件头部（JSON格式）
        - 支持`--checkpoint`和`--resume`用于长模拟
        - 输出文件包含：git commit hash、编译时间、编译器版本

    18.7. **测试规范**
        - 单元测试：GoogleTest框架，覆盖核心数学运算
        - 回归测试：已知温度点磁化强度与精确解/参考值对比
        - 压力测试：Valgrind内存检测、AddressSanitizer
        - 性能回归：Google Benchmark记录每次PR的核心操作耗时

    18.8. **文件组织**
        ```
        examples/
        ├── include/           # 公共头文件
        │   ├── mc_base.hpp
        │   ├── ising_model.hpp
        │   └── observers/
        ├── src/               # 实现文件
        │   ├── metropolis.cpp
        │   ├── swendsen_wang.cpp
        │   └── wolff.cpp
        ├── tests/             # 单元测试
        │   ├── test_energy.cpp
        │   └── test_cluster.cpp
        ├── benchmarks/        # 性能基准
        │   └── scaling_bench.cpp
        └── tools/             # 辅助工具
            ├── run_and_plot.py
            └── verify_onsager.py
        ```

    18.9. **代码审查检查点**
        - 是否满足细致平衡的实现（可数学验证）
        - 是否有未捕获的整数溢出（L×L > 2^31时）
        - 随机数生成器是否正确传递（非拷贝导致相同序列）
        - MPI并行版本（Part 3可能需要）：通信边界条件处理正确性

    18.10. **文档字符串格式（Doxygen兼容）**
        ```cpp
        /**
         * @brief 计算当前自旋构型的能量
         * @param spins 自旋数组，size = L*L
         * @param J 最近邻耦合常数
         * @param Jp 次近邻耦合常数（可选，默认0）
         * @return 总能量 E = -J Σ_⟨ij⟩ S_iS_j + Jp Σ_⟨⟨ik⟩⟩ S_iS_k
         * @note 周期边界条件
         * @complexity O(N) where N = L*L
         */
        double computeEnergy(const std::vector<int>& spins, double J, double Jp = 0.0);
        ```
    18.11. **编译器与语言版本选择**
        - 尽可能最新稳定版本，如c++23，gcc13，等诸如此类。
19. 忽略 baby* 这些文件，他们是作者用于自我教学的。
## 系列讲义编写计划
### 基本信息：
Author：Yongkang Xu
Institute/University：Centor of correlated matter, Zhejiang University
### 1-Classical MonteCarlo Algorithms
1. Motivation
TbCo2 为亚铁磁结构，Co的kagome晶格与Tb局域电子晶格（见pics/TbCo2.png and TbCo2Lattice.png）

我们期待用Kondo-Ising model 来描述这个体系的反常霍尔行为(TbCo2_transport_properties.png)（Kondo-Ising见refers/ 以及W.-W. Yang, J. Zhao, H.-G. Luo, and Y. Zhong, Exactly
solvable Kondo lattice model in the anisotropic limit, Phys.
Rev. B 100, 045148 (2019).
64 W.-W. Yang, Y. Zhong, and H.-G. Luo, Hexagonal
Ising-Kondo lattice: An implication for intrinsic anti-
ferromagnetic topological insulator, Phys. Rev. B 102,
195141(2020).
65 W.-W. Yang, Y.-X. Li, Y. Zhong, and H.-G. Luo, Doping
a Mott insulator in an Ising-Kondo lattice: Strange metal
and Mott criticality, Phys. Rev. B 104, 165146 (2021).）

（这里需要generate一个kagome + kondo-ising model的示意图，可以参考文中图片）

我们假设c-f hybridization 很弱，将体系投影到Kondo-Ising  model。
K-I model可以拆分为巡游电子部分——受温度影响只是fermi-dirac分布的smearing 和 局域电子部分——看做平均场，其大小受温度影响由Monte Carlo给出。
具体细节和应用会在更后续的slide给出。

2. MonteCarlo 概述
MC是用sampling的方法用离散的随机数来数值simulate某个分布。

计算机系统的随机种子生成序列下，计算过程本身也有更多有趣的问题，我们会在第二节进行讲述。

现在回到MC的两个条件。

细致平衡与遍历性（数学描述以及为什么这两个可以保证config与所需要的分布一致）

热化与热化成功的判别方法（e.g. 从全spin up和随机spin两个初始出发直到平衡，以及https://arxiv.org/abs/2604.10254 给出的overlap）

3. 经典算法-Ising对比
metropolis，SW，Wolff 三个算法的介绍与实例（介绍应当有详细的图例解释算法过程、标准算法流程描述、evaluator数学形式）以及细致平衡的证明：不同状态概率公式的写出。

实例选取二维正方格子、哈密顿量为`H = -JSS` and `H = -JSS + J'SS`两种，一个是最近邻铁磁，一个加了次紧邻阻矬。
实例对比在三种算法之后，使用cpp代码实现。由于slide前部以-JSS为示例解释三种算法，后续实例算法案例给出的时候应说明有阻矬的时候概率p如何变化，以及有没有通用的表示形式？
| 文件路径 | 内容说明 | 对应slide |
|---------|---------|----------|
| `examples/ising_base.hpp` | Ising模型基类（L×L晶格、NN/NNN耦合、抽象sweep接口） | Sec 3 通用 |
| `examples/metropolis.cpp` | Metropolis单自旋翻转实现 | Frame 3.2-3.3 |
| `examples/swendsen_wang.cpp` | SW算法（FK键激活+union-find找簇+簇翻转） | Frame 3.4-3.5 |
| `examples/wolff.cpp` | Wolff单簇算法（种子生长+边界拒绝） | Frame 3.6-3.9 |
| `examples/compare_algorithms.cpp` | 三算法对比（磁化率、自关联时间、T_c附近效率） | Frame 3.10 |
| `examples/CMakeLists.txt` | spack包管理配置（依赖：Eigen, Boost可选） | — |
| `examples/run_and_plot.py` | 运行代码+调用generatePic.py生成数值结果图 | Sec 3 数值实例 |

注意：三种算法应当有热化判定（可通用复用的判断函数），判定方式1:\< m \> 变化小。2.从all up和1/2 random两个初始config出发，两个过程的< m > 值收敛到一处。

4. Heisenberg 下如何使用几种算法
在三维（或XY二维）中，像wolff中“同方向”spin并没有直接对应，这时候mirror flip的引入可以保证过程像之前一样细致平衡。

5. 无限晶格下的wolff算法
参考 refers[0008072]，如前一样生成示意图并讲解，代入实例给出效果。

6. 临界慢化
以有NNN的Ising model（J1-J2 model）为例，给出Wolff和SW算法在该model下的实现。说明经典算法的临界慢化行为，

7. QMC
未来我们会讨论一些量子蒙卡方法，比如loop algorithm, worm, 这里简单介绍一下QMC和MCMC的区别以及为什么必须引入QMC...

8. 参考文献
（以学术会议报告标准引用格式按照每部分出现寻找对应文献并引用）

9. 下集预告
有学者表明，cluster overlap在Wolff和SW算法中有着和可观测量类似的统计行为。

我们会借用`cluster overlap`作为系统在算法中的演化状态量，构建时间序列，并利用动力系统的方式（takens，lyapunov，混沌吸引子，分形状态图）评价各个MCMC算法（chaos or random）
### 2-MC Algo in the view of a dynamic system（with noise）
1. Chaos and random
以三体运动和布朗运动为例，说明chaos是对初始参数敏感但系统有自己的径迹，random是某种分布，总可以用uniform分布得到
2. takens嵌入定理，相空间重构，Lyapunov指数（Rosenstein，替代数据检验），吸引子投影图像
并写出通用的hpp头文件，用于对几种算法取得的时间序列进行分析。（注意！这里需要判断是否用cpp，毕竟py有可能有完整的混沌分析库）
3. $ U_N $ of three Algo
refer to [2604.10254] 说明overlap的含义，以及作者们发现 U_2 有类似于物理可观测量的标度行为。
参考Analysis of ALL algorithms.ipynb 在之前的三个算法cpp文件中实现U_N为measure。
4. NN-Ising model的动力系统视角分析--生成时间序列
取therm = 0，Sweep = 1000000（对metropolis是sweep，对其他算法是对应的判断是否翻转次数）生成时间序列（专门研究从热化到平衡整个过程的复杂动力系统性质）
｜初始config｜all up以及1/2 random分布两种｜
｜   温度   ｜类似于generatePic中的温度分布，取0-4的点，Tc附近点密集一点｜
｜   算法   ｜metropolis，Wolff，SW｜
｜  观测量  ｜<m> 和 U_2 两种observable的时间序列｜
｜ 系统大小L ｜L=16，32，64，128｜
5. NN-Ising model的动力系统视角分析--混沌性分析
按照观测量-算法-初始config-温度-系统大小的顺序，进行混沌性分析。
- takens嵌入（互信息法求tau，假近邻法求m）
- Rosenstein法求最大Lyapunov指数，\lamda1
- 可视化重构相空间投影图（2D）、不同方法和参数lamda1对比等
6. 分时段Lyapunov指数


### 3-Application in Kondo-Ising model(1) Kondo-Ising Model and fitting from DFT+U
（BO and CMA-ES）

### 3-Application in Kondo-Ising model(2) MonteCarlo Result

### 4-Quantum MonteCarlo Method in impurity problem(1) Metropolis Algorithm for impurity problem

### 4-Quantum MonteCarlo Method in impurity problem(2)VMC、dQMC、DMC、FCIQMC
（变分蒙卡、行列式蒙卡、扩散蒙卡、全组态量子蒙卡）


### 当前编写进度与修改问题记录

| 日期 | 完成内容 | 备注 |
|------|---------|------|
| 2026-05-11 | Iteration 1: Beamer模板建立 | Madrid主题 + 自定义蓝灰学术配色，xelatex编译通过，8页骨架（含title、outline、6个section stub） |
| 2026-05-11 | Iteration 2: Kagome+Kondo-Ising示意图 | generatePic.py生成kagome_kondo_ising.png，Co kagome子晶格 + Tb局域自旋 + c-f杂化虚线，matplotlib实现 |
| 2026-05-11 | Iteration 2b: v2等轴侧视角优化 | 参考AM paper FIG.1，改为isometric side-view双层结构（Co kagome上层/Tb自旋下层），垂直Kondo耦合虚线，14页slides编译通过 |
| 2026-05-11 | Iteration 3: Section 1 动机部分 | 6页slides完成：TbCo2结构→输运性质→Kondo-Ising模型示意图→哈密顿量分解→MC路线图，v2图J_K连接修正为多邻居 |
| 2026-05-11 | Iteration 4: Section 2 开头 (Frames 2.1–2.2) | MC采样核心思想（期望值公式、Boltzmann分布、Markov chain概念TikZ图）+ 计算机随机数说明（PRNG、seed轨迹TikZ图），15页编译通过 |
| 2026-05-12 | Iteration 5: Section 2 完成 (Frames 2.3–2.4) | 细致平衡与遍历性（数学公式 + 固定点/唯一性论证 + TikZ双向流图）+ 热化与判别方法（burn-in流程图 + overlap引用arXiv:2604.10254），17页编译通过，无Overfull |
| 2026-05-12 | Iteration 6: Section 3 Metropolis部分 (Frames 3.1–3.3) | 2D Ising模型引入（NN+NNN哈密顿量、正方格子TikZ图）+ Metropolis算法（acceptance概率、流程图TikZ）+ 详细平衡证明（Case 1/2分类论证 + 遍历性说明），19页编译通过 |
| 2026-05-12 | Iteration 7: Section 3 Swendsen-Wang部分 (Frames 3.4–3.5) | FK键激活概率公式 + SW算法步骤（bond激活→union-find找cluster→独立翻转）+ 手工自旋构型TikZ图（Cluster A/B标注）+ 详细平衡证明（FK表示下Boltzmann权重消去），21页编译通过 |
| 2026-05-12 | Iteration 7b: SW图表修复 + backup推导页 | 修复TikZ自旋构型（改为清晰的上下两层矩形cluster，up/dn style一次创建避免矩形覆盖）+ 补充SW详细平衡完整推导backup slide（Step 1-4两栏布局），22页编译通过 |
| 2026-05-12 | Iteration 7c: SW示意图升级为6×6 | 参考SW_example.png，改为6×6对角畴壁构型（↑左上/右下，↓右上/左下），4个cluster（A/A'/B/C）+ 随机键断裂标记(×)展示SW随机性（A'由A的stochastic split产生），22页编译通过 |
| 2026-05-12 | Iteration 7d: SW详细平衡推导修正 | 修正三个backup frame的数学错误：(1) Frame 3：转移概率公式添加兼容性条件$B\sim S,S'$，添加FK形式常数消去说明和遍历性论证；(2) Frame 4 Step 1：δ_ij→b_ij并显式约束仅对齐键，Step 4：用\underbrace标注key lemma代换；(3) Frame 5重写：原P(S,U)不变性证明有根本错误（边界键贡献不守恒），改为正确的π(S)·P(B|S)不变性证明（核心恒等式$e^{βJ}(1-p)=e^{-βJ}$），24页编译通过，无Overfull |
| 2026-05-12 | Iteration 8: Section 3 Wolff部分 (Frames 3.6–3.9) | Wolff单簇算法（核心概念+TikZ流程图）+ 5×5 cluster生长TikZ示例（seed标记/激活键/拒绝键×/cluster轮廓）+ 详细平衡证明（边界键不变性+核心恒等式）+ backup推导（两栏Step 1-4）+ 三算法对比表（Metropolis/SW/Wolf: z/更新范围/复杂度/适用场景），28页编译通过，无Overfull |
| 2026-05-12 | Iteration 8b: Wolff详细平衡修正 | 修正两个问题：(1) page23/24文字重叠与超出block — 重写frame结构，移除负vspace导致的溢出，Ergodicity移入exampleblock内，确保所有文字在灰色背景内；(2) 数学错误 — 转移概率$1/N$改为正确的$|C|/N$并补充内键因子$\prod_{\mathcal{I}}p$；ratio推导去掉SW的FK恒等式$e^{\beta J}(1-p)=e^{-\beta J}$，改为Wolff直接论证：aligned边界键forward贡献$(1-p)$ vs reverse贡献$1$，用$(1-p)=e^{-2\beta J}$匹配$\Delta E=2J$每键；backup Step 3修正$W(S'\to S')$为$W(S'\to S)$，Step 4简化为单行结论避免重叠，28页编译通过，minimax验证无重叠 |
| 2026-05-12 | Iteration 8c: Wolff转移概率语义澄清 | 主frame添加"$W(S\to S')\neq 0$ only when $S'$ is obtained by flipping one connected cluster"说明单步可达性限制；backup frame添加"Key difference from SW"导言：Wolff每步仅翻转一个cluster，$W(S\to S')=0$ unless $S'$ differs by a single cluster flip，任意两config间可能需要多次Wolff step，但detailed balance按每步独立验证；主frame公式改为inline避免Overfull，28页编译通过，minimax验证无重叠 |
| 2026-05-12 | Iteration 8d: Wolff Step 4完整推导 | 拆分原backup为两页：(1) 原backup保留Step 1-3（转移概率分解、消去公因子、边界键分析），Step 3末尾改为引导至下一页；(2) 新增独立backup页"Verify Detailed Balance"：Step 4代入$(1-p)=e^{-2\beta J}$展开ratio为$(e^{-2\beta J})^{n_\uparrow}(e^{2\beta J})^{n_\downarrow}$；Step 5写明边界能量$E_S|_{\partial C}=-Jn_\uparrow+Jn_\downarrow$和$E_{S'}|_{\partial C}=-Jn_\downarrow+Jn_\uparrow$（swap），给出$\Delta E=2J(n_\uparrow-n_\downarrow)$；结论exampleblock：$e^{-\beta\Delta E}=e^{-2\beta J(n_\uparrow-n_\downarrow)}=W(S\to S')/W(S'\to S)$，29页编译通过，minimax验证无重叠 |
| 2026-05-14 | Iteration 9: Frame 6双图布局 + 计划格式规范化 | Frame 6改为左右双栏（左：kagome_kondo_ising.png 2D视图，右：kagome_kondo_ising_v2.png 3D等轴侧视图），添加height=0.78\textheight约束避免Overfull，29页编译通过，无Overfull，minimax验证无重叠；规范化编写原则编号（19→18）及代码块格式 |
| 2026-05-14 | Iteration 10: Batch 0 C++基础设施 | 创建目录结构(include/src/tests/benchmarks/tools/)；mc_base.hpp(CRTP框架+ObservableRegistry+MCSimulation)；ising_model.hpp(2D Ising L×L, NN+NNN, 能量计算, 观测器注册)；metropolis.cpp(完整Metropolis实现)；CMakeLists.txt(C++23)；README.md；techTutorial.md；修复dangling reference bug(MCSimulation改为引用而非所有权)；16×16 T=2.0测试|m|≈0.92符合预期 |
| 2026-05-14 | Iteration 11: rng_seed_修复 + generatePic数据绘图 + Metropolis验证图 | 修复ising_model.hpp rng_seed_未初始化bug；mc_base.hpp添加writeTimeSeries()方法；generatePic.py添加数据绘图基础设施(parse_mc_output/run_mc_binary/temperature_sweep/onsager_exact_M) + plot_metropolis_mt()；16个温度点(L=16, 5000 sweeps)扫描生成pics/metropolis_mt.png，MC数据与Onsager精确解吻合，T_c附近有限尺寸圆滑效应符合预期，minimax验证通过 |
| 2026-05-15 | Iteration 11b: 热化收敛检测 | mc_base.hpp添加ThermConfig/ThermResult/autoThermalize()(滑动窗口平台检测)；metropolis.cpp添加--auto-therm(自动热化)、--ts(时间序列输出)、--all-up修复(原bool传参取反)；generatePic.py添加plot_thermalization()(双初始态⟨m⟩轨迹收敛图)；--auto-therm在T=2.5时2900 sweeps收敛；pics/thermalization_convergence.png MiniMax验证通过 |
